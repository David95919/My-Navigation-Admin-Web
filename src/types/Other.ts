export type IpInfo = {
  country: string
  short_name: string
  province: string
  city: string
  area: string
  isp: string
  net: string
  ip: string
  code: number
  desc: string
}

export type Address = {
  city: string
  area: string
}
